import 'package:flutter/material.dart';

class ExprienceComponent extends StatelessWidget {
  const ExprienceComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
